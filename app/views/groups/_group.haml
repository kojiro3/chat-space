= link_to group_messages_path(group) do
  %li.groups__list
    %p.groups__list__name
      = group.name
    %p.groups__list__message
      = group.messages.last ? "#{group.messages.last.body}" : "まだメッセージはありません!!!"
